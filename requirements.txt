pydantic>=2.5
phonenumbers>=8.13
networkx>=3.2
# PDF parsing
PyMuPDF>=1.23
# OCR fallback (optional - requires Tesseract system install)
pytesseract>=0.3.10
Pillow>=10.0
# Language detection (seeded for determinism)
langdetect>=1.0.9
# Skill extraction
flashtext>=2.7
# Skill fuzzy canonicalization
rapidfuzz>=3.5
# NER engines (all optional - pipeline degrades gracefully if absent)
gliner>=0.1.0
spacy>=3.7
# Date normalization
python-dateutil>=2.9
# Name transliteration (multilingual support)
unidecode>=1.3.8
# Web server
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6
reportlab>=4.0
gunicorn
